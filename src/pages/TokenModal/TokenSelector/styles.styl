.tokenButton {
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
  padding: 0.5rem 1rem;
  border-radius: 10px;
  background-color: transparent;
  -webkit-font-smoothing: antialiased;
  -webkit-tap-highlight-color: transparent;
  cursor: pointer;
  transition: all 0.4s ease;
  outline: none;
  min-width: 149px;
}

.swapTokenImg {
  width: 35px;
  box-shadow: rgba(0, 0, 0, 0.075) 0px 6px 10px;
  margin-right: 0.5rem;
  border-radius: 50%;
}

.ripple {
  position: relative;
  overflow: hidden;
  transform: translate3d(0, 0, 0);

  &:after {
    content: '';
    display: block;
    position: absolute;
    width: 100%;
    height: 100%;
    top: 0;
    left: 0;
    pointer-events: none;
    background-image: radial-gradient(circle, #FF0000 10%, transparent 10.01%);
    background-repeat: no-repeat;
    background-position: 50%;
    transform: scale(10, 10);
    opacity: 0;
    transition: transform 0.5s, opacity 1s;
  }

  &:active:after {
    transform: scale(0, 0);
    opacity: 0.2;
    transition: 0s;
  }
}

.selectATokenButton {
  border-radius: 10px;
  font-family: 'Poppins',Arial, Helvetica, sans-serif;
  padding: 0.5rem 1rem;
  -webkit-font-smoothing: antialiased;
  -webkit-tap-highlight-color: transparent;
  border: none;
  cursor: pointer;
  transition: all 0.8s ease;
  outline: none;
  display: flex;
  flex-direction: row;
  align-items: center;
  > span {
    width: 60px;
  }
  @media only screen and (max-width: 753px) {
    font-size: 14px;
  }

  &:disabled {
    cursor: default;
  }
}

.selectATokenButton , .tokenButton {
  box-shadow: 0 0 6px 2px #cb9b51;
}

.selectATokenImg {
  width: 35px;
  margin-right: 0.5rem;
}

.selectATokenDownAngle {
  width: 12px;
  margin-left: 0.5rem;
  &.dark {
    filter: invert(1);
  }
}

.clearTokenButton {
  @extend .selectATokenButton;
}
.addCustomToken__button{
  @extend .selectATokenButton;
}
.tokenSelectorSearch {
  flex: 1;
  border-radius: 4px;
  border: 1px solid rgb(237, 238, 242);
  background: transparent;
  width: 100%;
  height: 46px;
  padding: 16px;
  font-size: 18px;
  outline: none;
  margin-bottom: 1rem;
  &.dark {
    background-color: #363636;
    color: white;
  }
  &.light {
    color: black;
  }

  &:focus {
    border: 1px solid rgba(179, 134, 57,.6);
  }
  &::placeholder{
    font-family: 'Poppins',Arial, Helvetica, sans-serif;
    font-size: 16px;
    color: #5F5F6B;
  }
}

.listTokens__container{
  height: 60vh;
  width: 100%;
  margin: 0;
  display: flex;
  justify-content: center;
  align-items: center;
  border-top: 1px solid #DEDEDE;
  border-bottom:1px solid #DEDEDE;
}
.listTokens__subcontainer{
  width: 100% ;
  padding: .5rem .5rem;
  height: 100%;
  margin: 0;
}
.light {
  &.selectATokenButton, &.tokenButton {
    // background: rgba(255, 255, 255, 0.1);
    color:#5F5F6B;
  }
  &.clearTokenButton{
    background: rgba(255, 255, 255, 0.5);
  }
  &.addCustomToken__button{
    background: #D6D6D6;
  }
}

.dark {
  &.selectATokenButton, &.tokenButton {
    background: rgba(255, 255, 255, 0.1);
    color:#DEDEDE;
  }
  &.clearTokenButton{
    background: rgba(255, 255, 255, 0.1);
  }
  &.addCustomToken__button{
    background: rgba(255, 255, 255, 0.1);
  }
}

.thumb{
  border-radius: 20px;
  &.dark{
    background: #555;
  }
  &.light {
    background: #ccc;
  }
}
#modal_container{
  width: 450px;
  @media only screen and (max-width: 753px) {
    width: 90%;
  }
}
#actions{
  @media only screen and (max-width: 753px) {
    padding: 1rem !important
  }
}
